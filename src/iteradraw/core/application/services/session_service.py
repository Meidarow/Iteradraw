class SessionService:
    """
        Image order and duration controller.

        Stores in a database the timer selected for a given image and the time it ws defacto shown for.
        Defines the order in which images are shown depending on whether the user selects to shuffle order
        or not. Persists Session order and durations in database through session_repo.
    """
def __init__(self):
    pass

def fetch_image():
    pass
